<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpikeHarness">

        <!-- The only UI: a launcher screen to enable the service + show adb usage. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The perception + actuation engine. Reads the active window's a11y tree
             and dispatches taps / text entry across other apps. -->
        <service
            android:name=".SpikeAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Driven from the host via `adb shell am broadcast -n <pkg>/.CommandReceiver`.
             exported=true so the adb shell UID can reach it. SPIKE / EMULATOR ONLY:
             any app on the device could also trigger these commands. -->
        <receiver
            android:name=".CommandReceiver"
            android:exported="true" />
    </application>
</manifest>
