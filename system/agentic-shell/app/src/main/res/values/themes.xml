<resources>
    <!--
      The UI is entirely Jetpack Compose, so the XML theme only needs to provide
      a window background, no action bar, and transparent system bars (the app
      draws edge-to-edge). Basing it on a platform theme avoids pulling in the
      com.google.android.material dependency.
    -->
    <style name="Theme.Amos" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/amos_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
