# TODOS — superseded

**This file is superseded by [`PLAN.md`](PLAN.md), the single source of truth**
(2026-06-23). Every item that lived here — shipped-work log, device adaptation,
semantic risk classifier, narration overlay, adaptive result UI, a11y-tree budget,
ambient-correctness eval, proactive battery budget, local model backend, multi-agent
hardening, reserves, and the resolved AOSP/Play-Integrity question — was absorbed
into PLAN.md's phases (§5), test pack (§6), and decision log (§8).

Do not add new items here; edit `PLAN.md` instead. Kept only so old links and git
history resolve.
